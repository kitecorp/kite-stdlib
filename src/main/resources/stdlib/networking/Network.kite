// Network - Abstract virtual network
schema Network {
    string name
    string cidrBlock
    boolean enableDnsSupport = true
    boolean enableDnsHostnames = true
    object tags
    @cloud(importable)
    string id
    @cloud
    string state
}

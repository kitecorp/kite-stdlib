// Bucket - Abstract object storage bucket
schema Bucket {
    string name
    string region
    boolean versioning = false
    object tags
    @cloud(importable)
    string id
    @cloud
    string arn
}

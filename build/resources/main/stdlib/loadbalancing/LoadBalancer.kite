// LoadBalancer - Abstract load balancer
schema LoadBalancer {
    string name
    string lbType = "application"
    boolean internal = false
    object tags
    @cloud(importable)
    string id
    @cloud
    string arn
    @cloud
    string dnsName
}
